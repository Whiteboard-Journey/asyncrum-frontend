export type Feature = {
    id: number;
    title: string;
    desc: string;
    image: string;
    features: string[];
};

export type LayoutDemo = {
    image: string;
    layout: string;
};

export type Service = {
    icon: string;
    title: string;
    description: string;
};
